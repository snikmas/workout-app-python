from src.classes.session import Session

class SessionManager:
    def __init__(self):
        self.session = Session(None, None)

    @staticmethod
    def check_session(self, session):
        print("we in the check session")
        return
        pass